<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.movieseeker">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".framework.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieSeeker">
        <service
            android:name=".framework.services.DetailIntentService"
            android:exported="false"></service>

        <receiver
            android:name=".framework.receivers.MainBroadcastReceiver"
            android:enabled="true"></receiver>
        <activity
            android:name=".framework.ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>